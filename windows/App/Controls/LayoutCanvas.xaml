<UserControl x:Class="BarelyReal.App.Controls.LayoutCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="200">
    <Border CornerRadius="14"
            Background="#FAFAFA"
            BorderBrush="#E0E0E0"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Canvas x:Name="StageCanvas" Grid.Row="0" Background="Transparent" SizeChanged="Stage_SizeChanged" />

            <TextBlock Grid.Row="1"
                       Text="Drag the Mac screen to either side of this PC."
                       Foreground="#8A8A8A"
                       FontSize="11"
                       Margin="14,0,14,8"
                       HorizontalAlignment="Left" />
        </Grid>
    </Border>
</UserControl>
